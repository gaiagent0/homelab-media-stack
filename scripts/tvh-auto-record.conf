# tvh-auto-record.conf — Configuration for tvh-auto-record.sh
#
# Add show titles below (one per line, quoted).
# The script will search the EPG for upcoming broadcasts and schedule recordings.
#
# Usage:
#   SHOWS=("Title 1" "Title 2" "Title 3")
#
# DVR_CONFIG can be set to a specific TVHeadend DVR config name
# (e.g., "Sport", "Movies", "Tvshows", "Kozelet")
# Leave empty for the default config.

declare -a SHOWS=(
    "Az elefántok titkai"
    # Add more shows below:
    # "Új Sorozat Neve"
    # "Film Cím"
)

# DVR config name (leave empty for default)
DVR_CONFIG=""
